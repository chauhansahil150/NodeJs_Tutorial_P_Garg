<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Url Shortner</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f9f9f9;
        }
       
nav {
    background-color: #007bff;
    padding: 10px 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
}

li {
    margin-right: 20px;
}

li:last-child {
    margin-right: 0;
}

a {
    color: #ffffff;
    text-decoration: none;
    font-size: 16px;
    font-weight: bold;
}

a:hover {
    text-decoration: underline;
}

.user-info {
    display: flex;
    align-items: center;
    color: #ffffff;
}

.user-info img {
    width: 30px;
    height: 30px;
    border-radius: 50%;
    margin-left: 10px;
}

/* Add this new style */
.user-info span {
    margin-right: 10px;
}


    
        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #333333;
        }
    
        form {
            display: flex;
            flex-direction: column;
            max-width: 400px;
            margin: 0 auto;
        }
    
        label {
            font-size: 16px;
            color: #333333;
            margin-bottom: 10px;
        }
    
        input[type="text"] {
            border: 1px solid #dddddd;
            border-radius: 4px;
            padding: 10px;
            font-size: 16px;
            margin-bottom: 15px;
            width: 100%;
        }
    
        button {
            background-color: #007bff;
            color: #ffffff;
            border: none;
            border-radius: 4px;
            padding: 12px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.2s ease;
        }
    
        button:hover {
            background-color: #0056b3;
        }
    
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 30px;
        }
    
        th,
        td {
            border: 1px solid #dddddd;
            padding: 10px;
            text-align: left;
        }
    
        th {
            background-color: #007bff;
            color: #ffffff;
        }
    
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
    
        tr:hover {
            background-color: #ddd;
        }

       
        
    </style>
</head>

<body>
    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/logout">Logout</a></li>
        </ul>
            <div class="user-info">
                <span>Welcome, User</span>
                <img src="path_to_your_image.jpg" alt="User Avatar">
            </div>
       
    </nav>
    <h1>URL-Shortner</h1>
    <% if (locals.id) { %>
     <p>Short url is : http://localhost:8001/url/<%= id %></p>
    <% } %>
    <div>
        <form action="/url" method="POST">
            <label for="">Enter Original Url</label>
            <input type="text" name= "url" placeholder="https://www.google.com"/>
            <button type="submit">Generate</button>
        </form>
    </div>
<div>
    <% if (locals.urls) { %>
        <table>
            <thead>
                <th>Sr NO.</th>
                <th>Short Id</th>
                <th>Redirect URL</th>
                <th>Clicks</th>
            </thead>
            <tbody>
                <% urls.forEach((url, index) => { %>
                <tr>
                    <td>
                        <%= index+1 %>
                    </td>
                    <td>
                        <p>http://localhost:8001/url/</p><%= url.shortId %>
                    </td>
                    <td>
                        <%= url.redirectURL %>
                    </td>
                    <td>
                        <%= url.visitHistory.length %>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    <% } %>
</div>
</body>

</html>